import type { ReactNode } from 'react';
export type ContentPageProps = {
    title: string;
    children: ReactNode;
};
export declare function ContentPage({ title, children }: ContentPageProps): import("react").JSX.Element;
