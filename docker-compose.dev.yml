services:
  postgres:
    ports:
      # Bind to localhost only so the dev database is reachable from the host
      # without exposing it to the LAN.
      - '127.0.0.1:${POSTGRES_HOST_PORT:-5433}:5432'
