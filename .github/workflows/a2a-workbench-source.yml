name: a2a-workbench-source
on:
  push:
    branches: [feat/a2a-console-onboarding]
    paths: [.github/workflows/a2a-workbench-source.yml]
permissions:
  contents: read
jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - run: git archive --format=tar.gz --output=/tmp/a2a-workbench-source.tar.gz HEAD
      - uses: actions/upload-artifact@v4
        with:
          name: a2a-workbench-source
          path: /tmp/a2a-workbench-source.tar.gz
          retention-days: 1
