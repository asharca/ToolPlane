name: a2a-remote-source
on:
  push:
    branches: [feat/a2a-console-onboarding]
    paths: [.github/workflows/a2a-remote-source.yml]
permissions:
  contents: read
jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - run: git archive --format=tar.gz HEAD > /tmp/a2a-remote-source.tar.gz
      - uses: actions/upload-artifact@v4
        with:
          name: a2a-remote-source
          path: /tmp/a2a-remote-source.tar.gz
          retention-days: 1
