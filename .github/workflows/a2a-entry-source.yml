name: a2a-entry-source
on:
  push:
    branches: [feat/a2a-console-onboarding]
    paths: [.github/workflows/a2a-entry-source.yml]
permissions:
  contents: read
jobs:
  source:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - run: git archive --format=tar.gz HEAD > /tmp/a2a-entry-source.tar.gz
      - uses: actions/upload-artifact@v4
        with:
          name: a2a-entry-source
          path: /tmp/a2a-entry-source.tar.gz
          retention-days: 1
